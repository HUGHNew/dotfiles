# Dotfiles

The main branch is for *Debian*.

> Feel free to run `startup.sh` to copy they to your own home

run install-base.sh at your own risk

install all brew-relative packages: `brew bundle install`
