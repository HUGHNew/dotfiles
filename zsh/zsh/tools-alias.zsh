# gitbook init
alias init="sudo gitbook init"
alias pdf="sudo gitbook pdf"
# Docker
alias Docker="sudo docker"
alias Attach="sudo docker attach"
alias Drm="sudo docker rm"
alias Dps="sudo docker ps"
alias Start="sudo docker start"
alias Stop="sudo docker stop"
alias Restart="sudo docker restart"


alias sicp="rlwrap scheme"

# prettyping
alias ping="~/prettyping"

alias fzf-open='xdg-open "$(fzf)"'

# for apps like this make the initial letter capital
# flatpak apps
# alias Emacs="flatpak run org.gnu.emacs" # Deprecated
alias Fiddle=electron-fiddle

